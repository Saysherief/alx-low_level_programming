#include "main.h"

/**
 * mul - multiplies two integers
 * Description - uses two integer parameters @a and @b
 * @a: input integer
 * @b: input integer
 * Return: integer multiplication of @a and @b
 */

int mul(int a, int b)
{
	int m  = a * b;

	return (m);
}
